---
import Base from '../../layouts/Base.astro';
import RequireAuth from '../../components/auth/RequireAuth.astro';
---

<Base title="Mis Pedidos">
  <RequireAuth />
  <section class="orders">
    <div class="orders-header">
      <a href="/cuenta" class="back-link">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M19 12H5M12 5l-7 7 7 7"/>
        </svg>
        Mi cuenta
      </a>
      <h1>Mis Pedidos</h1>
    </div>

    <div id="orders-loading" class="state-msg">Cargando...</div>

    <div id="orders-empty" class="state-msg" hidden>
      <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round">
        <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18M16 10a4 4 0 0 1-8 0"/>
      </svg>
      <p>Aún no tienes pedidos.</p>
    </div>

    <div id="orders-list" hidden></div>

    <div id="pagination" class="pagination" hidden>
      <button id="btn-prev" class="pg-btn" disabled>← Anterior</button>
      <span id="pg-info" class="pg-info"></span>
      <button id="btn-next" class="pg-btn">Siguiente →</button>
    </div>
  </section>
</Base>

<style>
  .orders {
    max-width: 760px;
    margin: 0 auto;
    padding: 32px 24px;
  }

  .orders-header { margin-bottom: 28px; }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-muted);
    margin-bottom: 10px;
    transition: color var(--transition);
  }
  .back-link:hover { color: var(--accent); }

  h1 {
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--text);
    margin: 0;
  }

  /* ── Estados ── */
  .state-msg {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 14px;
    padding: 80px 0;
    color: var(--text-muted);
    font-size: 0.95rem;
  }
  .state-msg p { margin: 0; }

  /* ── Orden card (global: el HTML se inyecta vía innerHTML sin hash de Astro) ── */
  :global(.order-card) {
    background: var(--bg-surface);
    border: 1px solid color-mix(in srgb, var(--text) 8%, transparent);
    border-radius: var(--radius-md);
    padding: 18px 20px;
    margin-bottom: 10px;
    transition: border-color var(--transition);
  }
  :global(.order-card:hover) {
    border-color: color-mix(in srgb, var(--accent) 35%, transparent);
  }

  :global(.order-top) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 12px;
  }

  :global(.order-meta) {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  :global(.order-id) {
    font-size: 0.8rem;
    font-weight: 800;
    color: var(--text);
    letter-spacing: 0.5px;
  }

  :global(.order-date) {
    font-size: 0.78rem;
    color: var(--text-muted);
  }

  :global(.order-bottom) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 12px;
    border-top: 1px solid color-mix(in srgb, var(--text) 7%, transparent);
  }

  :global(.order-details) {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  :global(.order-items-count) {
    font-size: 0.82rem;
    color: var(--text-muted);
  }

  :global(.order-tracking) {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--text-muted);
    letter-spacing: 0.3px;
  }

  :global(.order-total) {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--accent);
  }

  :global(.status-badge) {
    display: inline-flex;
    align-items: center;
    padding: 4px 12px;
    border-radius: 999px;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    white-space: nowrap;
  }
  :global(.status-PENDING)   { background: rgba(251,191,36,0.12);  color: #fbbf24; }
  :global(.status-CONFIRMED) { background: rgba(96,165,250,0.12);  color: #60a5fa; }
  :global(.status-SHIPPED)   { background: rgba(167,139,250,0.12); color: #a78bfa; }
  :global(.status-DELIVERED) { background: rgba(74,222,128,0.12);  color: #4ade80; }
  :global(.status-CANCELLED) { background: rgba(248,113,113,0.12); color: #f87171; }

  /* ── Paginación ── */
  .pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    margin-top: 24px;
  }

  .pg-btn {
    padding: 8px 18px;
    border: 1.5px solid color-mix(in srgb, var(--text) 15%, transparent);
    border-radius: var(--radius-sm);
    background: none;
    color: var(--text);
    font-family: inherit;
    font-size: 0.85rem;
    font-weight: 600;
    cursor: pointer;
    transition: border-color var(--transition), color var(--transition);
  }
  .pg-btn:hover:not(:disabled) { border-color: var(--accent); color: var(--accent); }
  .pg-btn:disabled { opacity: 0.35; cursor: not-allowed; }

  .pg-info { font-size: 0.85rem; color: var(--text-muted); }

  @media (max-width: 480px) {
    h1 { font-size: 1.4rem; }
    .order-total { font-size: 1rem; }
  }
</style>

<script>
  import { initAuth } from '../../stores/auth';
  import { getMyOrders } from '../../lib/api';
  import type { MyOrder } from '../../lib/api';

  initAuth();

  const loading    = document.getElementById('orders-loading')!;
  const empty      = document.getElementById('orders-empty')!;
  const list       = document.getElementById('orders-list')!;
  const pagination = document.getElementById('pagination')!;
  const btnPrev    = document.getElementById('btn-prev') as HTMLButtonElement;
  const btnNext    = document.getElementById('btn-next') as HTMLButtonElement;
  const pgInfo     = document.getElementById('pg-info')!;

  const STATUS_LABEL: Record<string, string> = {
    PENDING:   'Pendiente',
    CONFIRMED: 'Confirmado',
    SHIPPED:   'Enviado',
    DELIVERED: 'Entregado',
    CANCELLED: 'Cancelado',
  };

  function formatDate(iso: string): string {
    return new Date(iso).toLocaleDateString('es-CL', { day: '2-digit', month: 'short', year: 'numeric' });
  }

  function renderOrder(o: MyOrder): string {
    const shortId = o.id.split('-')[0].toUpperCase();
    const tracking = o.trackingCode
      ? `<span class="order-tracking">
          <svg xmlns="http://www.w3.org/2000/svg" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          ${o.trackingCode}
        </span>`
      : '';

    return `
      <div class="order-card">
        <div class="order-top">
          <div class="order-meta">
            <span class="order-id">#${shortId}</span>
            <span class="order-date">${formatDate(o.createdAt)}</span>
          </div>
          <span class="status-badge status-${o.status}">${STATUS_LABEL[o.status] ?? o.status}</span>
        </div>
        <div class="order-bottom">
          <div class="order-details">
            <span class="order-items-count">${o.items.length} ${o.items.length === 1 ? 'producto' : 'productos'}</span>
            ${tracking}
          </div>
          <span class="order-total">$ ${o.totalAmount.toLocaleString('es-CL')}</span>
        </div>
      </div>
    `;
  }

  let currentPage = 1;
  const LIMIT = 10;

  async function load(page: number) {
    loading.hidden = false;
    empty.hidden = true;
    list.hidden = true;
    pagination.hidden = true;

    try {
      const res = await getMyOrders(page, LIMIT);
      loading.hidden = true;

      if (!res?.data?.length && page === 1) {
        empty.hidden = false;
        return;
      }

      empty.hidden = true;
      list.innerHTML = res.data.map(renderOrder).join('');
      list.hidden = false;

      if (res.totalPages > 1) {
        pgInfo.textContent = `Página ${res.page} de ${res.totalPages}`;
        btnPrev.disabled = res.page <= 1;
        btnNext.disabled = res.page >= res.totalPages;
        pagination.hidden = false;
      }

      currentPage = res.page;
    } catch {
      loading.hidden = false;
      loading.textContent = 'No se pudieron cargar los pedidos.';
      empty.hidden = true;
    }
  }

  btnPrev.addEventListener('click', () => load(currentPage - 1));
  btnNext.addEventListener('click', () => load(currentPage + 1));

  load(1);
</script>
